import 'package:flutter/material.dart';

class ApplyLeave extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
        title: Text('ApplyLeave'),
    ),
    body: Center(
    child: Text('ApplyLeave'),
    ),
    );
  }
}
